# 安全政策

## 报告安全漏洞

如果你发现了安全漏洞，**请不要在公开 Issue 中报告**。请通过以下方式私下报告：

🔒 **GitHub Security Advisory** — 在仓库 Security 标签页选择 [Report a vulnerability](https://github.com/nous-hermes/weiwuweixin/security/advisories/new) 私下提交

请在报告中包含：

- 漏洞的详细描述
- 复现步骤
- 受影响的版本范围
- 可能的修复建议（如有）

## 处理流程

1. 我们将在 **48 小时内** 确认收到你的报告
2. 我们将在 **7 天内** 提供初步评估和预期修复时间
3. 修复完成后，我们将公开发布安全公告并致谢报告者（除非你要求匿名）

## 支持版本

当前仅支持最新发布版本。请确保你使用的是最新版本。

| 版本 | 支持状态 |
|------|----------|
| 0.1.x | ✅ 支持 |

## 安全最佳实践

如果你自己部署围物为心，请注意：

- **不要暴露 `.env` 文件** — 生产密钥应使用环境变量或密钥管理服务
- **修改默认 JWT_SECRET** — 不要使用模板中的默认值
- **配置 HTTPS** — 生产环境务必使用 TLS
- **定期更新依赖** — 运行 `pnpm update` 获取安全补丁
- **配置速率限制** — API 已内置 `@fastify/rate-limit`，生产环境请调整参数
- **数据库安全** — 使用强密码，限制网络访问范围
